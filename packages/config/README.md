# @wayno/config

Shared baseline tooling configurations across the Wayno monorepo:
- Base `tsconfig.json` definitions.
- ESLint rules and plugins.
- Prettier code formatting rules.
- Tailwind CSS design system presets and color tokens.
